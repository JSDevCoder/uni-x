/**
 * 合并Map数据
 * @param target 合并的目标数据
 * @param value 要合并的数据
 * @returns 合并完成新的数据
 */
export function merge(target: Map<string, string>, value: Map<string, string>): Map<string, string> {
	value.forEach((v, k) => {
		if(target.has(k)) {
			target.set(k, v)
		}
	})
	return target
}